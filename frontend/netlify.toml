[build]
  # explicit build command if you like:
  command = "npm run build"
  # leave publish = "" or omit publish for Next.js; Netlify handles routing for you.

# Example to pin specific plugin version (not required)
# [[plugins]]
#   package = "@netlify/plugin-nextjs"
